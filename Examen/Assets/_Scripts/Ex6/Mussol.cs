using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mussol : Animals
{

    public Mussol() : base() {}

    public override string Name => "Mussol";
    public override bool Fly => true;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
