using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gos : Animals
{

    public Gos() : base() {}

    public override string Name => "Gos";
    public override bool Fly => false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
